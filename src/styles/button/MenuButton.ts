import styled from '@emotion/styled';

const MenuButton = styled.button`
    border: none;
    background: none;
    cursor: pointer;
    padding: 6px 10px;
    border-radius: 12px;

    &:hover {
        background: #c2c2c2;
    }
`;

export default MenuButton